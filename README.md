# NETTE startup CMS

Základní redakční systém.

* composer install
```bash
composer install
```

* vytvořit /config/local.neon
```bash
parameters:


database:
dsn: 'mysql:host=localhost;dbname=DB_NAME'
user: DB_USER
password: DB_PASS
```

